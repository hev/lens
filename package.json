{
  "name": "lens-local-clip-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/worker.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
